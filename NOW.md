# NOW

Active
- Reconcile canonical and service documentation with current runtime reality
  (binds, auth semantics, web-search path, MLX runtime family) using verified
  systemd/listener/runtime evidence.

NEXT UP
- Run a post-cleanup consistency pass (`CONSISTENCY_DOD` checks + drift grep)
  and close remaining doc contradictions in non-canonical service notes.
