# NOW

Active
- Move the next UI/tool service batch into live `services/` paths.
- Update registry, templates, docs, and path-sensitive tests so the mixed-root
  migration remains deterministic.
- Keep this tranche narrow: no `openhands`, no control-plane runtime cluster,
  no `vector-db`.

NEXT UP
- Land this batch, then plan the remaining data/runtime moves in smaller
  tranches.
