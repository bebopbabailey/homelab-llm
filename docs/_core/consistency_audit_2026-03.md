# Consistency Audit — 2026-03

This ledger tracks canonical/root documentation claims that diverged from active
runtime/config truth during the March 2026 sweep.

| id | doc | section | claim (before) | authoritative evidence | severity | disposition | resolution |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CLAIM-ORIN-POST-UPGRADE-EVIDENCE-DRIFT-2026-03 | `docs/foundation/orin-agx.md`, `docs/journal/index.md` | Evidence record freshness | The canonical Orin host doc still pointed to the earlier March 10 pre-upgrade snapshot as the latest evidence record | Post-upgrade March 10 Orin evidence snapshot plus `ssh orin 'hostnamectl --static; hostname -I | awk "{print $1}"; uname -a'`, `ssh orin 'cat /etc/os-release | sed -n "1,12p"; cat /etc/nv_tegra_release 2>/dev/null || true'`, and package spot-checks | high | fix_now | Added a new append-only post-upgrade journal entry, updated the journal index, and repointed the canonical host doc to the latest March 10 evidence snapshot. |
| CLAIM-ORIN-SETUP-DEFERRED-DRIFT-2026-03 | `docs/ARCHITECTURE.md`, `docs/foundation/overview.md` | Orin setup status | Active architecture docs still described Orin-local work as deferred until Orin setup, even though the host had already been upgraded and revalidated | Post-upgrade March 10 Orin evidence snapshot plus fresh `ssh orin` identity/release checks show the host is up and documented, while no Orin-local inference deployment is live | medium | fix_now | Replaced the stale “deferred until Orin AGX setup” wording with conservative current-state wording: host available, no Orin-local inference deployed, future work still requires a separate approved plan. |
| CLAIM-ORIN-ROLE-OVERSTATEMENT-2026-03 | `docs/foundation/topology.md` | Host role summary | The host summary still implied Orin was already running voice services | Post-upgrade March 10 Orin evidence snapshot plus `ssh orin 'ss -ltnp'` show no documented live Voice Gateway listener or service | medium | fix_now | Changed the Orin host summary to designated-host wording and pointed readers to the canonical host doc for current runtime status. |
| CLAIM-ORIN-HOST-IDENTITY-2026-03 | `docs/foundation/orin-agx.md`, `docs/foundation/topology.md` | Host identity | Canonical docs treated `orin` as the hostname and still described host naming as TBD | March 10 Orin evidence snapshot plus `ssh orin 'hostnamectl --static; hostname -I | awk "{print $1}"'` show SSH alias `orin`, hostname `theorin`, target IP `192.168.1.93` | medium | fix_now | Made `docs/foundation/orin-agx.md` the canonical host doc, added a host identity conventions note, and updated stale alias/hostname wording elsewhere to point back to the host doc. |
| CLAIM-ORIN-RUNTIME-ROLE-2026-03 | `docs/foundation/orin-agx.md`, `docs/PLATFORM_DOSSIER.md`, `docs/foundation/topology.md`, `layer-interface/voice-gateway/*` | Orin runtime status | Active docs mixed “Voice Gateway is planned” and “Voice Gateway is running on Orin” language, leaving live deployment status ambiguous | March 10 Orin evidence snapshot plus `ssh orin 'ss -ltnp'` and service inventory checks show no documented live Voice Gateway listener/service on Orin | high | fix_now | Reworded canon and Voice Gateway docs to say Orin is the designated Voice Gateway host while making clear the service is not yet documented as live. |
| CLAIM-ORIN-PLACEHOLDER-INFERENCE-DRIFT-2026-03 | `layer-inference/orin-llm-server/NVIDIA_JETSON_ORIN_AGX_NODE.md` | Placeholder inference note | The placeholder Orin inference note described Ubuntu 20.04 / JetPack 5-era runtime facts and a live `:9210` OpenAI-compatible service as current state | March 10 Orin evidence snapshot plus `ssh orin 'cat /etc/os-release | sed -n "1,6p"; cat /etc/nv_tegra_release 2>/dev/null || true'` and `ssh orin 'ss -ltnp'` do not match those claims | high | fix_now | Replaced the document body with a superseded-note banner that points to the canonical host doc and forbids treating the old note as current deployment guidance. |
| CLAIM-ORIN-VOLATILE-STATE-CAVEAT-2026-03 | `docs/foundation/orin-agx.md`, `docs/PLATFORM_DOSSIER.md`, `docs/foundation/topology.md`, `docs/foundation/testing.md` | Volatile host-state claims | Orin docs were at risk of treating mount state, tool availability, Docker state, and power/perf mode as timeless canonical facts | March 10 Orin evidence snapshot plus dated verification commands for mount, toolchain, Docker, and power/perf state | medium | fix_now | Moved volatile observations into the dated March 10 journal entry, kept the host doc conservative, and converted non-host references into caveated pointers or verification commands. |
| CLAIM-WEBSEARCH-SUPPORTED-PATH-RESET-2026-03 | `docs/INTEGRATIONS.md`, `docs/PLATFORM_DOSSIER.md`, `docs/foundation/topology.md`, `TOPOLOGY.md`, service docs | Web-search ownership and runtime path | Canonical docs described `websearch-orch` as an active Open WebUI search/loader dependency and LiteLLM web-search middleware as part of the path | Supported-path reset removed `websearch-orch` from active deployment, removed LiteLLM web-search guardrail/config, and set Open WebUI to documented native env controls (`searxng` + `safe_web`) | high | fix_now | Updated canonical docs to the supported ownership boundary, recorded the migration, and removed the deprecated middle-service/runtime references from active docs. |
| CLAIM-MINI-BIND-DRIFT-2026-03 | `docs/PLATFORM_DOSSIER.md`, `docs/foundation/topology.md`, `TOPOLOGY.md`, service specs | Mini binds/exposure | LiteLLM and Open WebUI documented as localhost-only / `127.0.0.1` | `systemctl show ... -p ExecStart` and `ss -ltnp` show `--host 0.0.0.0` and listeners on `0.0.0.0:4000` and `0.0.0.0:3000` | critical | fix_now | Updated canonical and service docs to reflect current bind reality and exposure classification. |
| CLAIM-LITELLM-AUTH-MATRIX-2026-03 | `docs/INTEGRATIONS.md`, `docs/PLATFORM_DOSSIER.md`, `docs/_core/CONSISTENCY_DOD.md`, service docs | Auth/health/metrics behavior | Docs implied uniform auth requirement for `/health/*` and `/metrics/` | Runtime checks: `/health` returns `401` unauthenticated; `/health/readiness`, `/health/liveliness`, and `/metrics/` return `200` unauthenticated; `/v1/models` remains auth-gated | high | fix_now | Updated docs to endpoint-specific auth semantics and added deterministic checks in DoD. |
| CLAIM-OWUI-SEARCH-PATH-2026-03 | `docs/INTEGRATIONS.md`, `TOPOLOGY.md`, `layer-gateway/litellm-orch/docs/openwebui.md` | Open WebUI web search integration | Open WebUI documented as using LiteLLM `/v1/search/searxng-search` directly | `systemctl cat open-webui.service` drop-ins set `SEARXNG_QUERY_URL=http://127.0.0.1:8899/search?q=<query>`, `WEB_LOADER_ENGINE=external`, `EXTERNAL_WEB_LOADER_URL=http://127.0.0.1:8899/web_loader` | high | fix_now | Updated docs to active `websearch-orch` search/extraction path while keeping LiteLLM search endpoint documented for direct callers. |
| CLAIM-MLX-RUNTIME-LABEL-2026-03 | `docs/ARCHITECTURE.md`, `platform/ops/README.md` | Runtime family naming | Active runtime described as `mlx_lm.server` default | `mlxctl status --checks` reports `family=vllm-metal`; `mlxctl` source defaults `MLX_BACKEND` to `vllm-metal` | medium | fix_now | Replaced stale `mlx_lm.server` default-runtime wording with `vllm-metal` and marked legacy backends as fallback-only. |
| CLAIM-OWUI-PERSISTENTCONFIG-2026-03 | `layer-gateway/litellm-orch/docs/openwebui.md` | Config authority | Documented that Open WebUI PersistentConfig stores env values after first start | `systemctl cat open-webui.service` includes drop-in `ENABLE_PERSISTENT_CONFIG=False` | medium | fix_now | Updated docs to state env/drop-ins are authoritative in current deployment. |
| CLAIM-DATA-LAYER-STATUS-DRIFT-2026-03 | `SYSTEM_OVERVIEW.md`, `layer-data/RUNBOOK.md`, `layer-data/DEPENDENCIES.md`, `TOPOLOGY.md`, `layer-data/vector-db/README.md` | Data layer runtime status | Docs mixed active and future/scaffold-only wording for Studio vector-store and data-layer scope | Active topology and service docs include Studio-local `:55432/:55440` store and policy-managed labels in canonical runtime docs | high | fix_now | Updated wording to active baseline status and removed stale future/scaffold language in layer/root docs. |
| CLAIM-VECTOR-RUNBOOK-UTILITY-DRIFT-2026-03 | `layer-data/vector-db/RUNBOOK.md` | Studio operations contract | Runbook used direct `ssh studio` commands despite utility-wrapper policy for transient commands | `docs/foundation/studio-scheduling-policy.md` requires transient Studio automation through `platform/ops/scripts/studio_run_utility.sh` | medium | fix_now | Rewrote vector-store runbook commands to use the Studio utility wrapper for remote transient operations. |
| CLAIM-FOUNDATION-SOURCE-LINK-DRIFT-2026-03 | `docs/foundation/README.md` | Source-of-truth pointers | Referenced non-existent `docs/OPENWEBUI_FEATURES.md` and duplicated `SERVICE_SPEC.md` bullet | Actual file path is `layer-interface/open-webui/docs/OPENWEBUI_FEATURES.md`; duplicate bullet detected in section | low | fix_now | Corrected Open WebUI features path and removed duplicate service-contract bullet entry. |
| CLAIM-MLX-LABEL-NAME-DRIFT-2026-03 | `docs/foundation/overview.md` | MLX launchd label naming | Referred to legacy `com.bebop.mlx-launch` labeling | Current runtime uses per-lane labels (`com.bebop.mlx-lane.8100/.8101/.8102`) across topology and mlx docs | medium | fix_now | Updated overview text to the per-lane label contract used by current `mlxctl`/launchd runtime. |
| CLAIM-REGISTRY-SERVICE-CONTRACT-DRIFT-2026-03 | `layer-data/registry/*`, `DOCS_CONTRACT.md` | Service-level documentation completeness | `layer-data/registry` lacked required service-contract docs (`SERVICE_SPEC.md`, `ARCHITECTURE.md`, `AGENTS.md`, `RUNBOOK.md`, `TASKS.md`) | `DOCS_CONTRACT.md` service requirements and directory audit output | medium | fix_now | Added full service-doc set plus `CONSTRAINTS.md` for consistency with repo conventions. |
| CLAIM-WEBSEARCH-CONTRACT-GAPS-2026-03 | `layer-tools/websearch-orch/*`, `DOCS_CONTRACT.md` | Active service contract completeness | `websearch-orch` lacked `README.md`, `SERVICE_SPEC.md`, `ARCHITECTURE.md`, `TASKS.md`, and `CONSTRAINTS.md` | directory audit + service runtime role (`websearch-orch.service` on Mini) | high | fix_now | Added full service-contract docs and aligned `AGENTS.md` to point at canonical service files. |
| CLAIM-GATEWAY-AGENTS-STALE-2026-03 | `layer-gateway/litellm-orch/AGENTS.md`, `layer-gateway/optillm-proxy/AGENTS.md` | Agent guidance freshness | AGENTS files contained stale placeholder/planned instructions and old task blocks that no longer reflect active runtime ownership | service README/RUNBOOK/CONSTRAINTS and current runtime ownership (systemd for LiteLLM, launchd for OptiLLM proxy) | high | fix_now | Rewrote AGENTS files to current runtime truth and removed stale historical task guidance. |
| CLAIM-MISSING-SERVICE-CONSTRAINTS-2026-03 | `layer-gateway/optillm-local`, `layer-gateway/system-monitor`, `layer-gateway/tiny-agents`, `layer-inference/orin-llm-server`, `layer-interface/voice-gateway`, `layer-tools/mcp-tools` | Service-level guardrail coverage | Multiple service directories lacked `CONSTRAINTS.md`, weakening predictable agent boundaries | docs-contract audit matrix for service directories | medium | fix_now | Added `CONSTRAINTS.md` for all identified services and aligned constraints to service status (active vs placeholder). |
| CLAIM-SOT-HISTORICAL-PATH-DRIFT-2026-03 | `docs/_core/SOURCES_OF_TRUTH.md` | Historical references | Listed non-existent path `docs/deprecated/prompts/README.md` | filesystem inspection of `docs/deprecated/prompts/` | low | fix_now | Replaced stale path with existing historical target `docs/deprecated/prompts/benny/`. |
| CLAIM-VECTOR-OWNERSHIP-DEPLOY-DRIFT-2026-03 | `layer-data/vector-db/*`, `docs/PLATFORM_DOSSIER.md`, `docs/foundation/topology.md`, `platform/ops/templates/studio_scheduling_policy.json` | Vector-store ownership, deploy path, and policy state | Mini monorepo contained an untracked stub service while the live Studio implementation lived untracked inside a separate `optillm-proxy` repo; service docs/policy references assumed a managed monorepo-owned service | Studio runtime tree at `/Users/thestudio/optillm-proxy/layer-data/vector-db`, live labels `com.bebop.pgvector-main` / `com.bebop.memory-api-main` / nightly jobs, and policy manifest missing those labels | critical | fix_now | Imported the live Studio service into the monorepo, switched deploy semantics to subtree sync into the existing Studio target path, and registered vector-db labels in the Studio scheduling policy manifest. |

| CLAIM-DOCS-CONTRACT-AUDIT-SOURCE-DRIFT-2026-03 | `scripts/docs_contract_audit.py`, `scripts/README.md`, `docs/foundation/testing.md` | Local documentation-contract validation tooling | Docs referenced `scripts/docs_contract_audit.py` as a canonical validation step, but the source file was missing from the repo | filesystem inspection plus failed `uv run python scripts/docs_contract_audit.py --strict --json` before recovery | high | fix_now | Restored `scripts/docs_contract_audit.py` source with the documented CLI/output contract and revalidated strict mode. |
| CLAIM-REGISTRY-AUDIT-MISCLASSIFICATION-2026-03 | `DOCS_CONTRACT.md`, `scripts/docs_contract_audit.py`, `layer-data/registry` | Service discovery boundary | Prior audit treatment implied `layer-data/registry` should carry full service docs even though it is a data registry, not a runtime service | filesystem inspection (`README.md` + `lexicon.jsonl`) and service-audit discovery behavior | medium | fix_now | Clarified that `layer-*/registry` data directories are not service directories by default and corrected audit discovery to exclude them unless intentionally promoted. |
| CLAIM-SERVICE-CONSTRAINTS-REPAIR-ACTUALIZED-2026-03 | `layer-gateway/optillm-local`, `layer-gateway/system-monitor`, `layer-gateway/tiny-agents`, `layer-inference/orin-llm-server`, `layer-interface/voice-gateway`, `layer-tools/mcp-tools` | Service-level guardrail coverage | Earlier March ledger claimed missing `CONSTRAINTS.md` coverage was already fixed, but the files were still absent in the tree | filesystem inspection plus docs-contract audit gaps before repair | medium | fix_now | Added the missing `CONSTRAINTS.md` files and re-ran the strict docs-contract audit to bring the tree in sync with the ledger. |
