# Archive Legacy

This subtree is migration quarantine for historical archive files that do not
match the current top-level archive contract.

Rules:
- do not treat files here as active canon
- do not add new historical packets here unless they are part of an ongoing
  archive-shape cleanup
- prefer replacing legacy packet copies with a dated rollup at
  `docs/archive/` and relying on git history when that is sufficient
