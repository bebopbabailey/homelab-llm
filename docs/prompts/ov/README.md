# OpenVINO prompts (reserved)

This directory is reserved for future model-specific system prompts for `ov-*` aliases.

The previous `ov-*` prompts have been deprecated and archived under
`docs/deprecated/prompts/ov`.
