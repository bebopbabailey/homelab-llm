# Gateway Layer docs

Use this folder for:
- System monitor design notes
- Escalation/bulletin schema drafts
- Gateway routing policies
- Handles registry notes (see `layer-gateway/registry/handles.jsonl`)

Planned: bulletin stored in the system documentation DB (SQLite first).
