# OptiLLM Local (Deferred)

This directory is a **placeholder** only.

OptiLLM local inference is **deferred to the Orin AGX (CUDA)** and is not
running on the Studio. The prior Studio-local setup and launchd artifacts
have been archived in `docs/archive/optillm-local/`.
