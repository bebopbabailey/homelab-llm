# Tasks: llama-cpp-server

- No open service-local tasks remain after the shared `8126` GPT contract,
  upstream-formatting ownership boundary, and validation guidance were codified.
- Future runtime-family changes or separate raw-mirror promotion work should be
  tracked as new scoped slices, not reopened here implicitly.
