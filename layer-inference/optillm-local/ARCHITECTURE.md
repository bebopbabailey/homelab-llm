# Architecture: optillm-local

Historical architecture note: this document describes the older local
`mlx-lm`-based execution path.

Current active experimentation is the `vllm-metal` tuning flow on `812x`
experimental ports; see `README.md`, `SERVICE_SPEC.md`, and `RUNBOOK.md` first.
