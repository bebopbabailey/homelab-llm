# Tasks: orchestration-cockpit

## Phase 4
- Prove a local browser cockpit can start a graph-backed mission.
- Show the deterministic route decision in the chat flow.
- Keep the ordinary branch as a placeholder, not a commodity model call.
- Invoke the validated `omlx-runtime` adapter only for `/specialized <fixture-id>` missions.
- Keep the entire prototype localhost-only and non-production.
