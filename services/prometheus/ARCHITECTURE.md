# Prometheus (Architecture)

Prometheus is installed as a system package on the Mini and runs as
`prometheus.service`.

This repo provides configuration under `config/` that is deployed to
`/etc/homelab-llm/prometheus/` (see `SERVICE_SPEC.md`).

