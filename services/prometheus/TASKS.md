# Tasks: prometheus

## Next
- Add/adjust scrape config for LiteLLM `/metrics/` (use `/metrics/` with trailing slash; keep bearer header configured for forward compatibility).
- Add alerts only after baselines are stable.
