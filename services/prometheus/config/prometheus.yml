global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "litellm"
    metrics_path: /metrics/
    scheme: http
    authorization:
      credentials_file: /etc/homelab-llm/prometheus/litellm_bearer_token
    static_configs:
      - targets: ["127.0.0.1:4000"]
